# Control-plane dependencies: what the MCP server itself needs to run locally.
#
# Deliberately does NOT include jax. The serving stack runs on the G4dn
# instance, not here, and its CUDA dependencies would be wrong (and enormous) on
# a control-plane host. Those live in requirements-serving.txt and are installed
# by cloud-init; tests/test_server.py asserts the two agree. The profiler is a
# third list again -- requirements-profiling.txt, also instance-side.
mcp
httpx
boto3
