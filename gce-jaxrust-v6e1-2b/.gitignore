# Models and large files
gemma-4-*/
gemma-*/
*.safetensors
*.bin
*.tar.gz
*.pth
*.onnx

# private doc
tpu.md

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
env/
venv/
.venv/
pip-log.txt
pip-delete-this-directory.txt
.tox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
.hypothesis/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# OS / IDE
.DS_Store
.vscode/
.idea/

# Local MCP registration (generated by init.sh / project-setup.sh; contains the GCP project id)
.mcp.json

# Local environment (contains live API keys)
.env


# Rust build artifacts (Cargo.lock IS committed — these are binaries, and the
# engine pins rlx exactly because rlx-gemma pins rlx-runtime exactly)
rust/target/
