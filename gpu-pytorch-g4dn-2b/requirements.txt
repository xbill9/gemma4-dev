# Control-plane dependencies: what the MCP server itself needs to run locally.
#
# Deliberately does NOT include torch. The serving stack runs on the G4dn
# instance, not here, and its dependencies are CUDA wheels that would be wrong
# (and enormous) on a control-plane host. Those live in requirements-serving.txt
# and are installed by cloud-init; tests/test_server.py asserts the two agree.
mcp
httpx
boto3
