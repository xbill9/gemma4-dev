# Serving dependencies, installed ON the G5g instance by cloud-init.
#
# Not installed locally: jax[cuda13] resolves to aarch64 CUDA wheels for a T4G.
# server.py's _SERVING_REQUIREMENTS is the source of truth for what the bootstrap
# actually installs; this file mirrors it and a test asserts they match.
#
# jax[cuda13] pulls CUDA from pip (cublas, cudnn, cuda-runtime, cusolver all
# publish aarch64 wheels), so the DLAMI only has to supply the driver.
# Requires Python >= 3.12; the rig installs the newest stable line (see tpu.env).
jax[cuda13]
fastapi
uvicorn
pydantic
transformers
# Chat-template rendering for transformers. A missing jinja2 leaves /health
# green and fails every generation with a 500 (measured 2026-08-19).
jinja2
safetensors
huggingface_hub
numpy
