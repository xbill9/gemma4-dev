# local-pytorch-cpu-2b — install into the system python3. Never a virtualenv.
#
# torch IS NOT PINNED HERE and that is deliberate. This host's torch is whatever
# the other local work last needed — 2.13.0+cu130 as of 2026-09-04, pinned there
# by vLLM 0.28.0, which requires torch==2.13.0 exactly. Pinning a version in this
# file would silently fight that. The rig records what it RAN on in its
# benchmark reports instead.
torch
transformers>=5.0
# device_map needs it; without it from_pretrained raises ValueError.
accelerate
mcp>=1.2.0,<2
python-dotenv>=1.0.0
