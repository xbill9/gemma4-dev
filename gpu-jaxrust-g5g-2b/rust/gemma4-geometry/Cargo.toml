[package]
name = "gemma4-geometry"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Gemma 4 E2B model geometry and the KV/padding invariants, host-side and GPU-free."

[dependencies]
half = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
