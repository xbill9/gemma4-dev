# cover provenance — devto-cover.c9eb2ee2.jpg
# generated 2026-09-11T11:53:44-04:00 with gemini-3.1-flash-lite-image via google-genai 2.22.0 models.generate_content (Vertex AI, location=global)
# raw 1376x768 -> 1376 wide -> crop box (0, 80, 1376, 658) -> 1376x578 (2.381:1) -> JPEG q90
# name = sha256(bytes)[:8]
# prompt:
# Wide banner illustration for a technical blog cover, dark charcoal background with a faint blueprint grid. Bold clean white
# sans-serif headline centered near the top: "MCP SDK 2.x on AWS". Below it, a polished isometric 3D scene in the middle of the
# frame, reading left to right: on the left a glowing translucent blue server block with the label "FastMCP" beneath it; on the
# right a sleek dark graphite server blade with the label "MCPServer" beneath it. Between them a single bold glowing orange
# arrow that starts at the right side of the FastMCP block and ends with exactly one arrowhead touching the MCPServer blade,
# pointing right. There is no arrowhead at the FastMCP end, and no other arrows. On top of the blade sit a small plain square
# processor chip with a blank, unmarked surface and a compact GPU card with a plain unmarked shroud, both resting fully on the
# blade, joined by thin glowing orange data lines. Minimal, modern, high contrast, warm orange and cool blue accents, no
# clutter, no logos. The only text in the entire image is the headline and the two labels "FastMCP" and "MCPServer"; nothing
# is written on the chip or the GPU card. Keep all important content inside the central horizontal band, with generous empty
# margin at the top and bottom edges.

# cover provenance — builder-cover.5bcd37c5.jpg
# generated 2026-09-11T11:52:03-04:00 with gemini-3.1-flash-lite-image via google-genai 2.22.0 models.generate_content (Vertex AI, location=global)
# raw 1376x768 -> 1200x675 -> JPEG q90
# name = sha256(bytes)[:8]
# prompt:
# Wide banner illustration for a technical blog cover, dark charcoal background with a faint blueprint grid. A polished
# isometric 3D scene centered in the frame: on the left a glowing translucent blue server block; a bold glowing orange arrow
# flows right into a sleek dark graphite server blade. On top of the blade sit a small square processor chip and a compact
# GPU card, with thin glowing orange data lines between them. Minimal, modern, high contrast, warm orange and cool blue
# accents, no clutter. Absolutely no text, no letters, no numbers, no labels and no logos anywhere in the image.

# rejected dev.to renders, same pipeline, not committed:
#   c2369099  chip rendered an Arm-style wordmark over garbled text (asked for a "Graviton" label)
#   88904937  arrow drawn with two heads, reading as MCPServer -> FastMCP
#   5a26de8d  GPU card label rendered "T40", not "T4G"
#   47fb4654  GPU card label rendered "74G", not "T4G"
#   59e75d86  same prompt as the winner; headline crowded against the top edge
