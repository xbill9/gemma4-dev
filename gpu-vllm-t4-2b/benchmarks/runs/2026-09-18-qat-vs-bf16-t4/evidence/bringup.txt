# Bring-up of gpu-vllm-t4-2b, 2026-09-18, GCE n1-standard-2 + Tesla T4, us-west2-b.
# Verbatim tool output captured during the session, trimmed to the relevant lines.
# Where a figure came from an ad-hoc polling loop rather than a saved artifact,
# the loop is shown and the figure is labelled as such.

## 1. The install as found: CUDA vLLM on a CPU torch

$ /usr/bin/python3.13 -m pip list --user | grep -E "^(vllm|torch) "
torch                                    2.11.0+cpu
vllm                                     0.26.0
# vllm-0.26.0 METADATA: Requires-Dist: torch==2.11.0

## 2. After replacing torch with the cu130 build (verify_gpu_arch)

torch 2.11.0+cu130
torch.version.cuda 13.0
cuda available: True
arch list: ['sm_75', 'sm_80', 'sm_86', 'sm_90', 'sm_100', 'sm_120']
device: Tesla T4
capability: (7, 5)
shared mem per block (static): 49152
fp16 matmul ok: True

# First attempt used --no-deps and failed at import:
ImportError: libcudnn.so.9: cannot open shared object file: No such file or directory

## 3. Engine OOM-killed on host RAM (no swap), default loader

$ sudo dmesg | grep "Killed process"
[ 5431.169804] Out of memory: Killed process 14015 (VLLM::EngineCor) total-vm:27144376kB, anon-rss:3993204kB, file-rss:69212kB, shmem-rss:98540kB, UID:2110064466 pgtables:11488kB oom_score_adj:0
# Last engine log line before the kill:
(EngineCore pid=14015) INFO 09-18 14:36:20 [model_runner.py:284] Loading model from scratch...

## 4. --load-format fastsafetensors: a different failure, not a workaround

# Host memory peak sampled every 3 s from /proc/meminfo (MemTotal - MemAvailable):
elapsed=172s peak_host_used=7252MiB
(EngineCore pid=14545) Exception: [FAIL] framework hinted GPU runtime 'libcudart.so' but no GPU was found after loading it (runtime/devices for that vendor not present)

## 5. With a 16 GB swapfile on /opt1 (not in fstab)

$ sudo fallocate -l 16G /opt1/swapfile && sudo chmod 600 /opt1/swapfile && sudo mkswap /opt1/swapfile && sudo swapon /opt1/swapfile
Swap:          16383           0       16383

# Peaks sampled every 3 s from /proc/meminfo while the server started:
# vLLM 0.26.0 bf16:  time_to_healthy=476s peak_ram_used=5765MiB peak_swap=6205MiB
# vLLM 0.29.0 bf16:  time_to_healthy=531s peak_ram=5786MiB peak_swap=6761MiB
# vLLM 0.29.0 QAT:   time_to_healthy=478s peak_ram=5812MiB peak_swap=4770MiB
# (the 0.29.0 bf16 restart for the sweep, compile cache warm: bf16 healthy after 392s)

## 6. Power cap during a 1,200-token single-stream decode (vLLM 0.29.0, bf16)

# nvidia-smi --query-gpu=utilization.gpu,clocks.sm,power.draw,clocks_throttle_reasons.active -lms 500
100 %, 1290 MHz, 70.99 W, 0x0000000000000004
100 %, 1290 MHz, 69.97 W, 0x0000000000000004
100 %, 1275 MHz, 70.05 W, 0x0000000000000004
# nvidia-smi -q -d PERFORMANCE:  SW Power Cap : Active
# 0x4 is SW power cap. Max SM clock is 1590 MHz (gpu.txt); power.max_limit is 70.00 W.
# CPU during the same decode (top): EngineCore 18-20% of one core, API server 1-3%.

## 7. QAT spot checks at temperature 0 (vLLM 0.29.0, Marlin)

17*23 -> 391
facts -> The capital of Australia is Canberra and the chemical symbol for gold is Au.
# Not an accuracy evaluation. It rules out a kernel producing garbage, nothing more.

## 8. Engine-side prefix-cache hit rate during the sweeps (from the engine's 10 s logger)

# Summarised over every logger line of each server's lifetime (run/vllm.log):
# QAT  server pid 22678: max 53.1%  mean 7.97%  samples 380
# bf16 server pid 33733: max 62.5%  mean 8.17%  samples 398
# Every cell used a unique seed, so this was not expected. Source NOT identified:
# candidates are `vllm bench serve`'s initial single-prompt test run and its 2
# warmups repeating the cell's first prompt, or repeats inside the random dataset.
# Both builds used identical seeds, so the A/B holds; absolute TTFT may be low.
#
# NOTE: the logger's "Avg prompt throughput: 409.6 tokens/s" is NOT a prefill rate.
# It is 4096 tokens / the 10 s window, and appears during 512-token cells too.
